// for off - utilizado em arrays // 

const num = [2, 4, 6, 8, 10, 12]

for (let numbers of num) { // for of basicamente assim como o FOR percorre todo o looping da array adicionando o valor na var (numbers) 
    console.log(`Os numeros pares até 10 são: ${numbers}`)
}